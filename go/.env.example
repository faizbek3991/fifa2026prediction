# Address the server listens on
ADDR=:8080

# Path to the SQLite database file (created automatically)
DB_PATH=users.db

# Only used when running with -seed-admin-username to create/update a user:
#   SEED_PASSWORD=changeme go run . -seed-admin-username=admin
SEED_PASSWORD=
